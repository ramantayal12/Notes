# Connecting Prometheus to Grafana (Docker Setup)

## Steps

1. Open **Grafana UI** → go to **Connections → Data sources**.
2. Select your **Prometheus** data source (or create a new one).
3. In the **URL** field, enter:

http://prometheus:9090

4. Click **Save & Test** to verify the connection.
